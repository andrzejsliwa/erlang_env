{deps, [
        {yamler, ".*",
         {git, "https://github.com/goertzenator/yamler.git", {branch, "master"}}}
       ]}.

{lib_dirs, ["deps"]}.

%% Which files to cleanup
{clean_files, [
               "erl_crash.dump"
              ]}.